import React from 'react';
import useTitle from '../../extra/useTitle';

const Home = () => {
    useTitle('Home')
    return (
        <div>
            home page
        </div>
    );
};

export default Home;