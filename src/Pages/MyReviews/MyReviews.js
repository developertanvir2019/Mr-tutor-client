import React from 'react';
import useTitle from '../../extra/useTitle';

const MyReviews = () => {
    useTitle('MyReview')
    return (
        <div>
            <h1>all reviews</h1>
        </div>
    );
};

export default MyReviews;